<li id="{{guid}}" class="accordion-navigation {{class}}">
	{{#if isDataModel}}
		<ux-anchor>{{title}}</ux-anchor>
        <ux-content>{{{content}}}</ux-content>
	{{else}}
    	{{yield}}
	{{/if}}
</li>
