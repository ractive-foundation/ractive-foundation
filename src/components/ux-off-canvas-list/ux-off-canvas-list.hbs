<ul class="off-canvas-list">
    {{#items}}
        <li>
            {{#if this.href}}
                <a href="{{this.href}}" {{#if this.target}}target="{{this.target}}"{{/if}}>
                    {{{this.label}}}
                </a>
            {{else}}
                <label>{{{this.label}}}</label>
            {{/if}}
        </li>
    {{/items}}
</ul>