<ul id="{{guid}}" class="accordion {{class}}" data-accordion>
	{{#if isDataModel}}
		{{#items}}
			<ux-accordionitem datamodel="{{.}}"></ux-accordionitem>
		{{/}}
	{{else}}
    	{{>content}}
	{{/if}}
</ul>
