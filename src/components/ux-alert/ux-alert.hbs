<div data-alert class="alert-box {{class}}" aria-live="assertive" role="alertdialog">
	{{#if isDataModel}}
		{{{text}}}
	{{else}}
		{{yield}}
	{{/if}}
	<a href="#" class="close" on-tap="closeClicked">&times;</a>
</div>