<div class="{{class}}">
    <nav class="top-bar {{#if isExpanded}}expanded{{/if}}" data-topbar role="navigation" data-options="{{dataOptions}}">

        <ul class="title-area">
            <li class="name">
                <h1><a href="#">{{title}}</a></h1>
            </li>
            <li class="toggle-topbar menu-icon">
                <a href="#" on-tap="toggleThis">
                    <span>{{menuLabel}}</span>
                </a>
            </li>
        </ul>

        <section class="top-bar-section">
            <!-- Right Nav Section -->

            {{#if isDataModel}}

                {{#if rightItems}}
                    <ux-topbar-container class="right">
                        {{#rightItems}}
                            <ux-topbar-item
                                class="{{#if hasForm}}has-form{{/if}} {{#if active}}active{{/if}}"
                                href="{{href}}">
                                {{label}}
                            </ux-topbar-item>
                        {{/rightItems}}
                    </ux-topbar-container>
                {{/if}}


                <!-- Left Nav Section -->
                {{#if leftItems}}
                    <ux-topbar-container class="left">
                        {{#leftItems}}
                            <ux-topbar-item
                                class="{{#if hasForm}}has-form{{/if}} {{#if active}}active{{/if}}"
                                href="{{href}}">
                                {{label}}
                            </ux-topbar-item>
                        {{/leftItems}}
                    </ux-topbar-container>
                {{/if}}

            {{else}}

                {{>content}}

            {{/if}}

        </section>
    </nav>
</div>
